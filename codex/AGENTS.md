# AGENTS.md (Codex Version) — Secomm Launchpad

> Codex reads the **same single source of truth** as Claude Code: [`../.ai/AGENTS.md`](../.ai/AGENTS.md). Read it first — it is authoritative for all rules. This file is a thin **Codex-specific overlay**; it does **not** restate AGENTS.md. (Same pattern as root `CLAUDE.md`.)
> Generated by Secomm Production AI Toolkit (v4) on 2026-07-14.

## Read first

`.ai/AGENTS.md` is the single source of truth. Before emitting any diff, apply:

- **§4** Context Pack — read only the `project-context/` subset your task class needs (selective, not all files).
- **§7.1** Planning-first sequence + **§7.2** Coding Standards (full body in `project-context/CODING_RULES.md`).
- **§8.2** AI Must-NOT + **§8.3** Pre-review checklist — run the checklist before outputting code.
- **§11 / §12** Escalation + High-Risk Areas. If the task touches a high-risk area (Mollie/VNPAY payment, Mageplaza OSC checkout, TableRate shipping, order, customer data, DB schema, auth, the Hyvä Packagist token, deployment scripts), output `⚠️ ESCALATION REQUIRED: {area} — Tier 2 (CTO/SA)` and stop.
- **§10** Quality Gates.

## Safe-change rules (Codex-specific reminders)

- Read the ticket, spec, and the relevant `project-context/` subset before coding.
- Keep changes minimal — modify only what the ticket asks for; do **not** refactor outside the requested scope.
- Do **not** introduce new dependencies without TL approval.
- Do **not** modify high-risk areas (`.ai/AGENTS.md` §12) without escalation.
- Follow existing code patterns in the project (PSR-12 / Magento standard; Hyvä + Tailwind v4 + Magewire on the frontend).

## Testing

- Suggest unit tests for any new functions.
- Run existing tests before submitting: `vendor/bin/phpunit`.
- Follow existing test patterns in the project.

## Outcome-oriented execution

Continue through internal phases (research → plan → implement → self-review) in one run, stopping **only** at a real decision (plan/code/QC approval) or a high-risk area. Do **not** present an intermediate result (research note, plan, test output) and wait for "continue" when nothing blocks. See `.ai/AGENTS.md` §8.5 (Outcome-Oriented Execution) + §8.6 (Execution Efficiency).

## Codex positioning

**Best for**: Bug fixes (<4h), implementation plans, unit tests, code analysis, focused refactoring, code review.
**Not suited for**: Full workflow orchestration, multi-system architecture review, full legacy codebase scanning, long-running multi-file generation.

<!-- BEGIN Secomm Hyva skills routing (auto-generated by project-ai-upgrade --capability hyva; safe to remove) -->
## Hyva skills (stack_variant = magento-hyva)

Official Hyva dev-skills are installed at `.claude/skills/dev/hyva-*/` (read the relevant `SKILL.md` before Hyva frontend / theme / module work); Secomm governance at `.ai/integrations/hyva/SECOMM_OVERLAY.md`. Invoke by **natural language** (do not assume slash invocation): "Create a Hyva child theme", "Create a CSP-compatible Alpine component", "Compile Tailwind CSS for the active Hyva theme", "Render a responsive media image", "Create a Magento module for Hyva frontend work", "Turn approved Figma foundations and design-token exports into the Hyva global style foundation (Tailwind v4 CSS-first)". Apply Hyva skills only to Hyva-owned surfaces (respect the Hyva↔Luma fallback boundary). Hyva UI / Hyva CMS / Playwright / style-foundation skills are present only when those capabilities are installed (the style-foundation skill requires a Tailwind v4 CSS-first theme — never a Tailwind v3 / tailwind.config.js theme).
<!-- END Secomm Hyva skills routing -->
