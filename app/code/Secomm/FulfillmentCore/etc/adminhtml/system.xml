<?xml version="1.0"?>
<!--
  ~ @author Secomm Team
  ~ @copyright Copyright (c) 2026. Secomm All rights reserved (https://www.secomm.vn)
  ~ See COPYING.txt for license details.
  -->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Config:etc/system_file.xsd">
    <system>
        <section id="fulfillment_core" translate="label" sortOrder="40" showInDefault="1" showInWebsite="1" showInStore="1">
            <label>Fulfillment Core</label>
            <tab>secomm</tab>
            <resource>Secomm_FulfillmentCore::config</resource>
            <group id="general" translate="label" sortOrder="10" showInDefault="1" showInWebsite="1" showInStore="1">
                <label>General</label>
                <field id="enabled" translate="label comment" type="select" sortOrder="10" showInDefault="1" showInWebsite="1" showInStore="1">
                    <label>Enable outbound order export</label>
                    <source_model>Magento\Config\Model\Config\Source\Yesno</source_model>
                    <comment>Master switch for every OMS adapter (Pancake POS and later adapters). When No, the place-order observer and the 10-minute retry cron do not push any order. Each adapter still has its own enable flag. Default: Yes. No external data is required.</comment>
                </field>
            </group>
            <group id="export" translate="label" sortOrder="20" showInDefault="1" showInWebsite="1" showInStore="1">
                <label>Export retry</label>
                <field id="max_attempts" translate="label comment" type="text" sortOrder="10" showInDefault="1" showInWebsite="1" showInStore="1">
                    <label>Max export attempts</label>
                    <comment>How many times Magento retries a failed outbound push. Source: local config only (not read from Pancake). Cron secomm_fulfillmentcore_retry_failed_export runs every 10 minutes and retries rows in secomm_fulfillment_export with push_status pending or failed until this count. Default: 5. After the limit, fix the adapter error (for example warehouse_unmapped) then the row is not retried automatically.</comment>
                    <validate>validate-digits validate-greater-than-zero</validate>
                </field>
            </group>
        </section>
    </system>
</config>
