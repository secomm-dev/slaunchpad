<?php
/**
 * @author Secomm Team
 * @copyright Copyright (c) 2026. Secomm All rights reserved (https://www.secomm.vn)
 * See COPYING.txt for license details.
 */

/** @var \Secomm\PaymentCore\Block\ContinuePaymentButton $block */
/** @var \Magento\Framework\Escaper $escaper */
?>
<?php /** FEAT-CSWYEJ — Continue Payment POST form (spec §4.8). Guard-enforced server-side. */ ?>
<form action="<?= $escaper->escapeUrl($block->getActionUrl()) ?>"
      method="post"
      class="mt-4 flex justify-start">
    <input name="form_key" type="hidden" value="<?= $escaper->escapeHtmlAttr($block->getFormKey()) ?>" />
    <input name="order_id" type="hidden" value="<?= $escaper->escapeHtmlAttr((string)$block->getOrder()->getEntityId()) ?>" />
    <button type="submit"
            class="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white hover:bg-primary-dark">
        <?= $escaper->escapeHtml(__('Continue Payment')) ?>
    </button>
</form>
