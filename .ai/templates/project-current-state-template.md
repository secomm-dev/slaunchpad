# Project Current State (Durable)

<!-- Generated by Secomm Production AI Toolkit (Phase 1c). Nằm tại: .ai/project/CURRENT_STATE.md -->
<!-- DURABLE project truth (RM-07/§7.6): what has been built, what is known, active risks, decisions made. -->
<!-- KHÔNG phải session cursor — session "now" nằm ở .ai/runtime/session/ (Phase 1d). File này phản ánh trạng thái DỊNH của project, cập nhật khi record → Done. -->
<!-- KHÔNG store secrets, PII, full logs. Project-owned — never overwritten by toolkit sync. -->

> **Owner**: TL/SA · **Last consolidated**: {YYYY-MM-DD} · **Verified against commit**: {sha}

## What has been built

- {component / capability — link CMP-* row trong `COMPONENT_INDEX.md`; one line each, stable}
- ...

## Known & decided (durable)

- {decisions đã chốt — link DEC-XXX; không restatement body}
- ...

## Active risks / known limitations (summary — full list trong `project-context/06`)

- {risk affect project posture — link CMP-* / DEC-*}
- ...

## Documentation freshness (verification metadata)

- `project-context/01-12` carry `last_verified` / `verified_against_commit` / `status` (xem `core/verification-metadata.md`). Files `current` = credible; `suspect`/`stale` = scoped re-audit needed.
- ...

## Current focus / phase

- {active Phase / work-item IDs — brief; chi tiết trong records}
- ...

<!--
This is the DURABLE snapshot. Update qua `update-project-state` function khi một record → Done
(RM-07 frontmatter flags: changes_project_state / changes_architecture / changes_integration / changes_known_limitations).
Không ghi session scratch ở đây — session state ở .ai/runtime/session/ (Phase 1d).
-->
