# Project Current State (Durable)

<!-- Generated by Secomm Production AI Toolkit (Phase 1c). Durable project truth (RM-07/§7.6). -->
<!-- NOT the session cursor — session "now" lives in .ai/runtime/session/ (Phase 1d). -->
<!-- Project-owned — never overwritten by toolkit sync. Updated via update-project-state when a record → Done. -->

> **Owner**: TL/SA · **Last consolidated**: 2026-08-28 · **Verified against commit**: 2b5e01c5

## What has been built

- **Secomm Launchpad** — Magento 2.4.8-p5 + Hyvä 3.x storefront (vi_VN primary), fresh init (single commit `1afdfc8`).
- **CMP-ADDR** — Vietnam hierarchical address capture (dual-theme Luma+Hyva); customer form + cart estimation done, resolver hardening deferred (AC-011/Q1). → FEAT-YVN39K (done).
- **CMP-ADDR profile engine (FEAT-2PZQKJ + FEAT-YA2C0W)** — Phase 1 hoàn tất + Phase 2 renderer chờ QC (TASK-3T3NSV): recursive hierarchy (`parent_city_id`/`code` + membership `secomm_address_profile_location`), Address Profile/Schema XML engine merge cross-module, GraphQL `addressLocations`/`addressSchema` (ID-canonical, cacheable — cache varies by store, verified), profiles `vn_current`/`vn_legacy` + membership seeded; Hyva schema-driven renderer `schema-edit.phtml` behind flag `address/general/renderer` (default `legacy`; dev bật `schema` 2026-08-26). TL-approved 2026-08-26: TASK-9AEAQQ/NW66H9/J49PRZ/R83FXW completed.
- **CMP-STOREFRONT** — admin-configurable storefront announcement toggle (Yes/No → render block). → FEAT-YESRCX (done); FEAT-KQ6WC4 (broader proposal, proposed).
- **CMP-CHECKOUT / CMP-THEME / CMP-PAYMENT-MOLLIE** — Mageplaza OSC, Hyvä child themes, Mollie (active) installed; no delivery work records yet.
- **CMP-SECOMM-UI** — shared Hyvä-only Magento widget capability with dynamic Admin authoring and all 21 approved Batch 1 components. Module defaults and product-theme override resolution pass the final compatibility gate; no theme-specific POC override is shipped. → FEAT-J06WXZ / TASK-ZQ9ZE1 / TASK-XY9RZF.
- **Project AI Toolkit** — Phase 1a (canonical records) + 1b (per-work-item mode + decision assessment) + 1c (durable project state) + 1d (runtime/durable split) artifacts present; 1e/1f semantic satisfied via AGENTS §8.5/§8.6/§9. **Phase-1 contract synced 2026-07-23** (CHANGELOG 1.3.0): migration markers P1A–P1F stamped, validator + registry + record templates current @ toolkit 2026-07-22, 5 drifted skills + 2 runtime guides synced.

## Known & decided (durable)

- DEC-7 Strategy B (Hyvä-native) · DEC-8 module/Launchpad boundary · DEC-9 dual-theme (Luma+Hyva) — address module.
- DEC-FEAT2PZQKJ-001 — recursive hierarchy + XML profiles + membership subtree-claim; ID-canonical; D2 BC-first (accepted 2026-08-25).
- DEC-FEATYA2C0W-001 — VN_CURRENT/VN_LEGACY profiles, legacy codes `L-`, relation table 2 chiều (accepted 2026-08-25).
- DEC-FEATHEHJQ4-001..006 — VNPAY IPN hardening decisions (status `proposed`; SA/TL to accept): signature, status mapping, retry, idempotency, audit storage, payload versioning.
- Tier-2 escalation required for payment/checkout/security/schema/contract (AGENTS §11/§12).

## Active risks / known limitations (summary — full list in `project-context/06`)

- **CMP-VNPAY `suspect`** — IPN hardening pending (FEAT-HEHJQ4 `proposed`); timing-unsafe signature, status-based dedup (double-invoice race), no payment-transaction record, no audit trail. Do NOT activate VNPAY for production before FEAT-HEHJQ4 lands.
- **Search engine not configured** — Magento 2.4.8 requires OpenSearch (local env missing).
- **Production infrastructure undefined** — no Redis/Varnish/OpenSearch/CI committed; `env.php` is local-dev only.
- **AC-011/Q1 deferred** — address resolver `@deprecated` + `@cache(false)` → potential N+1.
- **Schema-mode interim gaps (known, per user scoping 2026-08-26)** — chỉ customer form tiêu thụ profile; cart/OSC/admin vẫn surface legacy (labels cũ; OSC City trên Region là core order + i18n legacy). Owner: TASK-YQSS3M (cart) / TASK-FMAN1B (OSC) / TASK-9EX975 (admin, deferred). Empty-dataset region = form dead-end (known design, chờ QC W4 ghi nhận).

## Documentation freshness (verification metadata)

- `project-context/01-09_*.md` carry `last_verified` / `verified_against_commit: 1afdfc8` / `status: current` (Phase 1c). `current` = credible; `suspect`/`stale` → scoped re-audit.
- AI trusts `current` docs; re-audits only `suspect`/`stale` (avoids whole-project audit).

## Current focus / phase

- **Secomm UI Phase 1 / Batch 1 complete** — Batch 2 catalog-backed providers/components remain intentionally deferred until separately requested.
- **Phase 1a–1d artifacts done**; **1e/1f** semantic-only (satisfied in AGENTS §8.5/§8.6/§9 + `rules/no-duplicate-knowledge.md` — no separate artifact). Migration markers P1A–P1F stamped 2026-07-23 (CHANGELOG 1.3.0).
- **Address batch chờ release**: TASK-3T3NSV tại QC gate (browser + L3) → sau đó commit; kế tiếp TASK-YQSS3M (cart) → TASK-FMAN1B (OSC).
- **Phase E-A done (chờ TL review)** — TASK-AQT7V3: ShippingCore address-resolution contracts (capability / resolved result 4-status REUSE VietNamAddress / context / external resolver + zero-provider pool / manager interface không impl); module.xml += Secomm_VietNamAddress; 19 unit tests; compile + validator OK (15 FAIL validator cũ pre-existing). 0 carrier code đổi. GHN fallback cứng 1456/21511 + `is_develop_mode=1` re-verified còn live → cần BUG task riêng. E-B (orchestration) kế tiếp.
- **Phase E-B done (chờ TL review)** — TASK-5XDG1P: `ShippingAddressResolutionManager` implement `ShippingAddressResolutionManagerInterface` — orchestrate-only: delegate 100% canonical resolution qua `VnAdminAddressResolverInterface` (4-state + AMBIGUOUS không auto-select); non-VN bypass qua `Model\Address\Exception\UnsupportedDestinationException` (không status thứ 5, không abuse UNMAPPED — contract adjustment docblock-only trên manager interface, REPORT TL review cùng pre-review); missing canonical identity → UNMAPPED không gọi resolver; request-scoped cache key `sourceScheme|sourceUnitCode|targetScheme` (cache cả AMBIGUOUS/UNMAPPED/missing-identity); pool + textual fallback KHÔNG invoke. 24 unit tests (incl. manager↔resolver thật integration-level); compile + validator OK (15 FAIL pre-existing; 7 errors full-suite = Secomm_Tracking pre-existing). 0 carrier code. TL-review r1: `getReceiverText()` đã XÓA khỏi context contract (PII hygiene trước contract freeze — 0 consumer, manager/cache/4-state không đổi). E-C carrier integration (GHN-C per SPIKE-9Z231Q — blocker "manager không impl" đã gỡ) kế tiếp.
- **BUG-JBX3H9 done (chờ TL review)** — GHN hardcoded destination fallback ĐÃ XÓA (1456/21511 + develop-mode fake from 1457/21715/'Phường 17…' + config `is_develop_mode` xóa sạch); mapping unavailable ⇒ fail closed qua `GhnLocationMappingException` (rate → GHN method unavailable; sync → fail + log + MQ retry); 9 unit tests; grep production = 0 hit; compile/validator OK. GHN cần QC rate-path trên store thật khi bật. Canonical migration GHN = Phase E-C (chưa làm).
- **SPIKE-9Z231Q done v3 (chờ TL/SA review)** — architecture report `Secomm_Ghn` (v3 = clarification exact-old-ward). Chuỗi đã chốt: GHN operational = old-style p/d/w; **PRE_2025 = intermediate carrier-required, resolve ở FULL ward granularity (province+district+exact old ward — TL/SA verified từ GHN data/runtime; same-district optimization + district-granular REJECTED)**; `getRequiredScheme` semantics confirmed (=PRE_2025); GHN p/d/w = provider-specific; "2025→GHN current province + virtual district" NOT SELECTED (§16). **Reverse-mapping audit: ONE_TO_ONE 186 (5.60%) / ONE_TO_MANY 3.097 (93.26%, same-district vẫn AMBIGUOUS) / NO_MATCH 38 (DATA_GAP 9 / STRUCTURAL_ADMIN_CHANGE 11 / UNKNOWN 18) / INVALID 0; KPI = `FULL_PRE_2025_ADDRESS_RESOLVED` (5.60%; 90.88% district-granular chỉ là supporting statistic)**. Recommendation chốt: **A — KEEP FULL PRE_2025 RESOLUTION**. E-B v2 (pool invocation khi AMBIGUOUS/UNMAPPED) MANDATORY — priority blocker; result-contract đủ semantics, gap informational EXTERNAL_RESOLVED/EXTERNAL_FAILED flagged follow-up; **external resolver operationally required** — `Secomm_VietMap` ~~preferred candidate~~ **REMOVED FROM BACKLOG 2026-09-16 (yêu cầu geocode — architecture v8; không external resolver provider nào được planning)**; AMBIGUOUS residual → UNAVAILABLE → LEGACY_ADDRESS_FALLBACK theo Legacy RATE Strategy (§15.1, DEC-FEATYA2C0W-005); GHN-C KHÔNG còn blocked bởi VietMap; naming `GHN_OPERATIONAL_ADDRESS`/`GHN_PROVINCE_ID`/`GHN_DISTRICT_ID`/`GHN_WARD_CODE`; GHN-A proceed, GHN-B spec-only; **0 production code (v1+v2+v3)**. Report: `.ai/research/SPIKE-9Z231Q-secomm-ghn-canonical-architecture.md` + scripts/JSON trong `.ai/evidence/SPIKE-9Z231Q/`.
- **TASK-GS78X2 in_progress (finding F chờ TL/SA)** — Data Patch `RefreshVnAdminPre2025Snapshot2024` (replace PRE_2025 reference dataset bằng snapshot cuối 2024 63/696/10.035): patch + validator `$expectedOverrides` + 3 virtual types DONE, reuse 100% import stack; **fail-loud + transaction rollback PROVEN trên failure thật** (DB nguyên vẹn, patch_list sạch). **BLOCKER: snapshot CSV vi phạm uniqueness `(region|parent|name_vi)` — 6 cặp district trùng tên trong tỉnh (TP Cao Lãnh vs huyện Cao Lãnh… cấu trúc thật 2024; file cũ chỉ giữ 1 mỗi cặp)** → options: (A) regenerate CSV với disambiguation suffix, (B) nới contract cho snapshot. Không tự sửa CSV/contract. Evidence: `.ai/evidence/TASK-GS78X2/test-result.md`.
- **FEAT-FQWEQ3 scaffolded (Secomm_Ghn carrier adapter — implementation kế tiếp)** — Full Spec `SPEC-FEAT-FQWEQ3-secomm-ghn-carrier-adapter.md` (owner draft + supplements §52) + records: FEAT + TASK-RJFTPZ (GHN-A skeleton) / TASK-MZ2TCB (GHN-B master data + mapping, Tier-2) / TASK-FMBBSD (GHN-C rate — **blocker cập nhật 2026-09-16: VietMap REMOVED khỏi backlog (arch v8); cần Legacy RATE Strategy config (DIRECT_FALLBACK/MAP_THEN_FALLBACK — contracts đã ship TASK-5JQYMP) + shipping rate-provider slice (đã ship)**) / TASK-9Q5ZAK (GHN-D create/cancel/return — **BLOCKED: staging evidence `is_new_to_address` merged-ward + package/shipment slices**) / TASK-RR1ZFN (GHN-E webhook) / TASK-8019VC (GHN-F cutover). **DEC-FEATFQWEQ3-001 accepted 2026-09-10 (supersede MỘT PHẦN SPIKE-9Z231Q v3): Create Order dùng `is_new_to_address=true` + `GHN_ADMIN_2025` names; rating giữ full PRE_2025 resolution; sync master data + mapping cả 2 scheme (`GHN_ADMIN_2025` + `GHN_ADMIN_PRE_2025`)** — spike report §16 đã thêm update note. Parallel coexistence: carrier code `ghn` / route / topic / config `secomm_ghn/*` đều mới, legacy giữ tới GHN-F (parity gate §41).
- **GHN-A done (TL approve 2026-09-10)** — TASK-RJFTPZ: skeleton `Secomm_Ghn` (config `secomm_ghn/general/*` token encrypted + timeout; `GhnApiClient` envelope + empty-body quirk fail-closed; exception taxonomy `Provider*` ×7; capability `VN_ADMIN_PRE_2025`; `GhnLogger` token scrub; 59 tests). QC pending-env: setup:di:compile + sandbox smoke call (MySQL down).
- **GHN-B done code-wise (chờ TL review schema Tier-2)** — TASK-MZ2TCB: `secomm_ghn_address_unit` + `secomm_ghn_address_mapping` (whitelist đủ) · sync CLI legacy+v3 fetchers (endpoints/fields verified developer.ghn.vn 2026-09-10) · mapping matcher/suggester/auditor · resolver fail-closed PRE_2025 triple + 2025 names; 94 tests.
- **GHN-B2 (TASK-TBM30R) done code-wise (chờ TL review)** — **DEC-FEATFQWEQ3-002 accepted 2026-09-10: mapping lifecycle = versioned export → offline reviewed mapping → import/bootstrap; KHÔNG cơ chế nào tự tạo APPROVED** (MappingGenerator + rebuildScheme đã xóa; matcher = candidate tooling classification A). Exporter deterministic CSV + manifest; importers APPROVED-only fail-loud UPSERT (duplicate/dangling/conflict/scheme-pair validation); bundled `data/` bootstrap placeholder v0.0.0-empty (dataset thật = export → offline review → import → commit); audit +dangling/duplicate/production_ready; CLI `export|import|suggest` + `sync|audit`; UnitPersister dùng chung sync/import. 121 tests / 345 assertions; Secomm_Ghn 0 lỗi full-suite. QC AC-L12 khi MySQL + sandbox credentials.
- **GHN-B.2 phase 2 dev-complete (chờ TL review)** — **TASK-6TNKDH: authoritative dataset v1.0.0 bundled + validated trên DB thật, KHÔNG cần GHN API**: master 3,355 (2025: 34+3,321) + 12,772 (legacy: 65/726/11,981, trung thực API giữ provider duplicate), mapping 3,355 + 10,794 APPROVED (coverage 100%, keyed `VN_ADMIN_PRE_2025_SNAPSHOT_2024`), manifest 1.0.0 4/4 checksum khớp; DB: unit 16,127, mapping 14,149, integrity 0/0/0/0/0; audit 2 scheme 100% mapped + production_ready + stale=0; resolver smoke 21/21 (Kỳ Lừa→Tân Thanh · Đông Thành→Nhân Thành · Hòa Bình→910116 · Yên Sơn→910044 curated); bootstrap không API (0 config rows). Suite Ghn 129/210,339/0F/0E/**0 skip** (placeholder-gate gate active). 3 defect first-real-run đã fix trong scope: (1) bundled master bị DISABLED hoá 2 provider duplicate (vi phạm raw fidelity — khôi phục byte-identical export; curated thuộc mapping layer `PROVIDER_DUPLICATE_CURATED`); (2) `MappingCsv::METHODS` thiếu 13 method authoring (mở rộng additive); (3) audit enumerate canonical = 0 do defect DB provider `parent_code=NULL` (VietNamAddress-side, vẫn thuộc owning stream) → 14,149 false-STALE → wire `CanonicalCsvProvider` snapshot-aware vào matcher/auditor (GHN-scoped; virtual SuggesterMatcher gộp). Refresh e2e với API thật chờ credentials owner (QC AC-L12). Evidence: `.ai/evidence/TASK-6TNKDH/phase2/`.
- **TASK-FMBBSD (GHN-C) slice 1 dev-complete (chờ TL review)** — **PHASE A: GHN API Contract Matrix** đọc lại current docs (developer.ghn.vn/en/docs, 12 trang, 2026-09-11) + **sandbox validation live** (owner đã cấu hình credentials trong ngày): fee **service_type_id-only (2/5) VERIFIED live** (service_id đã chết — leadtime docs warn no-effect); **`is_new_to_address=true` + `to_district_name=""` VERIFIED live** (create L8TL6B bằng dataset names Kỳ Lừa→Tân Thanh, fee 68,200 == create total_fee); idempotency client_order_code VERIFIED (retry → cùng order_code); payment_type_id=1 accepted; cancel per-order result=true; **type-5 fee không items bị reject (items de-facto bắt buộc heavy)**; **AS docs vs sandbox CONFLICT** (mọi naming bị gateway từ chối → không đưa runtime, EXTERNAL/OPERATIONAL). **PHASE B: RATE slice** — `GhnRateCalculator` (handoff E-C0 → `GhnMappingResolver` Stage-2 → fee → `CarrierRateOutcome` E-C1; §12 boundary PROVIDER_MAPPING_MISSING ≠ CANONICAL_UNRESOLVED; auth/config/route/parcel → UNAVAILABLE, timeout/5xx/malformed → TECHNICAL_FAILURE) + `GhnParcel`/`GhnRateQuery` VOs + config `origin_district_id`; **P0 contract fix: payment_type default 2→1** (Magento đã thu phí tại checkout — 2 = double-charge); stale>0 giờ CHẶN production_ready (architecture §38) + test. Không Magento Carrier wiring (checkout Tier-2, slice kế). 19 tests mới (16 calculator + 2 config + 1 stale-gate). Evidence: `.ai/evidence/TASK-FMBBSD/` (contract matrix + sandbox validation).

- **SPIKE-YH439T done (chờ TL/SA review)** — carrier runtime handoff + failure semantics (audit code thật E-A/E-B/r1 + 4 carrier): gap then chốt = **chưa có DestinationContextBuilder** (`VnOperationalAddressResolver` 0 consumer, 0 canonical identity persisted, destination handoff chưa tồn tại — chỉ origin side đang live); đề nghị **Option B-minimal** (`DestinationContextBuilder` + `CarrierAddressHandoff` DTO ≤5 members + `CarrierAddressHandoffService` 1 method — carriers không tự reinterpret status/exception); non-VN exception dịch tại handoff (không leak); textual fallback = ShippingCore quyết ALLOW / carrier thực thi; canonical vs provider mapping failure tách bạch + failure-reason 6 mã; rate chuẩn GHTK-pattern (hide + log luôn, không fake rate — anti-pattern GHN `$shippingFee=10` + debug-only log ghi nhận xóa ở GHN-C); capability verdict ĐỦ; phases E-C0→E-F với **E-F (external) lên trước GHN-C** theo evidence 9Z231Q v2 (AMBIGUOUS 93.26%); OD-1..OD-6 chờ TL/SA. **0 production code.** Report: `.ai/research/SPIKE-YH439T-shippingcore-carrier-runtime-handoff.md`.
- **SPIKE-WHHEZV done (chờ TL/SA review)** — Mageplaza_TableRateShipping v4.0.8 làm fallback engine theo service level (audit source thật): **Case A/B/C đều YES** — `carriers/mptablerate/active` chỉ gate `collectRates()` (TableRate.php:181-183), calculation pipeline compose được từ public members (Method::isActive + Rate\Collection::filterByRequest + TableRate::getCartData/validateShippingGroup + Method::calculatePrice) KHÔNG qua collectRates; no-match = không rate (không 0-fee/default/first-match, TableRate.php:225-231). Kiến nghị: **Option A internal calculator**; FALLBACK_ONLY suppression = config `active=0` (chính, zero-code) + guard plugin aroundCollectRates (phụ); STANDALONE = native exposure, ownership bridge; **identity mapping = method_id (int PK)** (chính là method code Mageplaza dùng); service-level identities (EXPRESS/SAME_DAY/STANDARD) owned ShippingCore + interface mới `CarrierServiceLevelInterface` (không đụng capability contract); bridge mới **`Launchpad_MageplazaTableRate`** implement `FallbackRateProviderInterface` (optional single provider, D7 pool); fallback chỉ trigger cho temporary technical failures (matrix per-level + `fallback_enabled` switch; KHÔNG fallback cho canonical unresolved/mapping missing/business rejection); bridge config 3 mode FALLBACK_ONLY/STANDALONE/DISABLED; limitations ghi nhận: destination granularity = region/postcode (không ward), Method::isActive phụ thuộc customer session + customer_group rỗng = never-active gotcha. Dependency: ShippingCore ↛ Mageplaza ↛ Launchpad; carrier → ShippingCore only. Phases E-SL0 contracts → LT-BRIDGE-1 → E-SL1 orchestration (sau E-C0). Addendum superseded "single emergency table rate" của YH439T. **0 production code.** Report: `.ai/research/SPIKE-WHHEZV-mptablerate-service-level-fallback.md`.
- **Phase E-SL0 done (chờ TL review)** — TASK-XXBN5X: ShippingCore service-level + fallback contracts (SPIKE-WHHEZV/YH439T): `ShippingServiceLevel` (EXPRESS/SAME_DAY/STANDARD + assertKnown, không SLA hardcode/label-as-key) · `CarrierServiceLevelInterface` (carrier khai báo level; docblock known-limitation carrier-level declaration) · `Api\Fallback\{FallbackRateRequest,FallbackRate,FallbackRateProvider}Interface` + VOs (provider-neutral; 8 trường mapped từ audit Mageplaza; KHÔNG RateRequest/PII/method_id; amount>0 = enforce cấu trúc "no-match ⇒ null, không zero-fee"; KHÔNG metadata dump) · `FallbackRateProviderPool` (D7, zero/one provider, không chaining); DI array rỗng; grep 0 Mageplaza/Launchpad/RateRequest (docblock đã genericize); 20 unit tests; compile + validator OK (records 0 finding; 17 FAIL = 15 baseline + 2 từ BUG records stream song song; 7 errors full-suite = Secomm_Tracking pre-existing). 0 carrier/Mageplaza/Launchpad code. TL-review r1: REJECT hardcoded constants → dynamic model (`Api\ShippingServiceLevelInterface` + `Model\ServiceLevel\{ShippingServiceLevel VO, ShippingServiceLevelRegistry}` DI-array — ShippingCore không embed taxonomy, zero-level valid, validation dynamic qua registry; Launchpad tự seed EXPRESS/SAME_DAY/STANDARD ở composition sau; `fallback_enabled` không thêm vào definition (tách generic identity khỏi fallback policy)). 30 unit tests; compile + validator OK (records 0 finding). Kế tiếp: LT-BRIDGE-1 (bridge module) + E-C0 handoff + E-SL1 orchestration.
- **Phase E-C0 done (chờ TL review)** — TASK-T78YH6: carrier-facing address handoff (SPIKE-YH439T Option B-minimal): `DestinationContextBuilder` (Quote\Address + capability → context; bridge `VnOperationalAddressResolver::resolveFromRuntime` = **consumer production đầu tiên**, unresolved → null identity → UNMAPPED; targetScheme áp 1 điểm từ capability; candidateCodes luôn [] — không trust caller) → `CarrierAddressHandoffService` (builder → manager cached → catch `UnsupportedDestinationException` → handoff; single resolution path, không gọi resolver trực tiếp; external pool không invoke) → `CarrierAddressHandoff` VO (applicable/resolvedAddress/fallbackEligible/failureReason `UNSUPPORTED_DESTINATION`|`CANONICAL_UNRESOLVED`; không isResolved/getUnresolved/PII/provider-stage reasons; AMBIGUOUS/UNMAPPED → resolvedAddress null + fallback chỉ ALLOW). Stage 1 (canonical handoff) vs Stage 2 (provider mapping, carrier-owned) documented. 22 unit tests; scoped suite 261/786 OK; compile + validator OK (0 finding T78YH6). 0 carrier/Mageplaza/Launchpad code. Kế tiếp: E-C1 rate outcome + carrier consume handoff (GHN-C/GHTK-D/Ahamove-E) + E-SL1 orchestration.
- **Phase E-C1 done (chờ TL review)** — TASK-NAT3YV: carrier rate outcome semantics (contracts only): `Api\Rate\{CarrierRateInterface, CarrierRateOutcomeInterface}` + VOs — SUCCESS/UNAVAILABLE/TECHNICAL_FAILURE; impossible combos unconstructible (fail-fast) + factories + `isSuccessful()` guard; **CarrierRate amount >= 0 (zero HỢP LỆ — promotion; asymmetric có chủ đích với FallbackRate strictly-positive)** + currency nullable; 5 shared REASON constants (2 giá trị trùng handoff — translate giữ nguyên; PROVIDER_MAPPING_MISSING = UNAVAILABLE deterministic không fallback; SERVICE_UNAVAILABLE + TECHNICAL_ERROR); carrier-specific detailed reasons là free string carrier-owned; **auth/config failure → UNAVAILABLE, không bao giờ TECHNICAL_FAILURE** (fallback không che lỗi cấu hình merchant); service-level + carrier identity ĐỨNG NGOÀI outcome (Option B); classification raw provider errors = carrier adapter owned. TL-review r1: (A) `FallbackRate` amount >= 0 — zero valid explicit rate (asymmetric bị REJECT; null duy nhất nghĩa "no rate" — XXBN5X r2); (B) `Api\Failure\ShippingFailureReason` = ONE canonical owner 5 shared reasons — handoff + outcome bỏ duplicate constants, reference owner, bỏ parity test; rule mới: **status = orchestration semantic, reason = diagnostic** (không infer fallback từ reason string); `unavailable(null)`/`technicalFailure(null)` valid. 51+ tests scoped 283/832 OK; compile + validator OK (0 finding NAT3YV/XXBN5X). 0 carrier code. Kế tiếp: carrier adoption (GHN-C/GHTK-D/Ahamove-E) + E-SL1 orchestration (fallback gating theo STATUS).
- **Phase E-SL1 done (chờ TL review)** — TASK-32ACTR: service-level realtime rate aggregation (aggregation ONLY — không routing engine): `ServiceLevelRateAggregator::aggregate(code, outcomes)` — validate dynamic qua `ShippingServiceLevelRegistry::assertKnown` (unknown → LocalizedException; không hardcoded taxonomy); outcomes **keyed theo carrier code** (identity preserved — Option A keyed array, không wrapper DTO); matrix 8 hàng đúng (SUCCESS giữ rate + key + input order không sort; TECHNICAL_FAILURE set tín hiệu kể cả khi có SUCCESS — E-SL2 rule "any SUCCESS ⇒ no fallback" thuộc E-SL2; UNAVAILABLE bỏ qua; zero outcomes valid); **reason strings 0 lần đọc** (status authoritative); không `FallbackRateProvider` dependency (reflection guard test); Aggregate VO: successfulRates keyed + hasSuccessfulRate/hasTechnicalFailure/getOutcomeCount. 14 unit tests (LEVEL_A/LEVEL_B test-defined); scoped suite 297/871 OK; compile + validator OK (0 finding 32ACTR). 0 carrier/Mageplaza/Launchpad code. Kế tiếp: E-SL2 fallback decision (any SUCCESS ⇒ no fallback; chỉ TECHNICAL_FAILURE + fallback_enabled mở fallback) + carrier adoption.
- **Phase E-SL2 done (chờ TL review) — ShippingCore foundation COMPLETE (hard stop)** — TASK-M3ME32: fallback decision orchestration: `FallbackPolicyInterface` + `ConfigurableFallbackPolicy` (DI array `fallbackEnabledByLevel`, default DENY — opt-in per level); `ServiceLevelRateDecision` (REALTIME/FALLBACK/UNAVAILABLE + invariants + factories); `ServiceLevelRateOrchestrator::decide(code, aggregate, fallbackRequest)` — matrix §23 đủ 9 hàng: **SUCCESS suppresses fallback (provider never called)**; UNAVAILABLE alone không trigger; disabled level → UNAVAILABLE + ẩn realtime dù aggregate có SUCCESS (aggregate không bị sửa); provider gọi đúng 1 lần chỉ trên path enabled + no-success + technical-failure + policy-enabled; zero provider = UNAVAILABLE bình thường; **>1 provider = LogicException (ambiguity fail-fast, không first-wins — REPORT TL)**; zero-rate fallback = FALLBACK hợp lệ (r2); code/aggregate mismatch + unknown code fail-fast. 22 unit tests; scoped suite 319/933 OK; compile + validator OK (0 finding M3ME32). 0 carrier/Mageplaza/Launchpad code. **Flow foundation hoàn chỉnh: handoff → outcome → aggregate → decision; KHÔNG thêm generic architecture khi chưa có real-consumer evidence — kế tiếp: carrier adoption (GHN-C/GHTK-D/Ahamove-E) + LT-BRIDGE-1 (Launchpad TableRate bridge) validate contracts thật.**
- **LT-BRIDGE-1 done (chờ TL review)** — TASK-NQT782: `Launchpad_MageplazaTableRate` (module Launchpad đầu tiên có tests; phpunit config += Launchpad testsuite): implement `FallbackRateProviderInterface` qua pipeline nội bộ Mageplaza (Method load/isActive → RateCollection filterByRequest với RateRequest bridge-only + cartData từ scalars → Rate::calculatePrice → CalculateRule combine — KHÔNG collectRates); mapping service-level→method_id qua DI (không admin dynamic rows — §R3); 2 mode FALLBACK_ONLY (default; active=0 + guard plugin warning khi misconfig) / STANDALONE (native, provider trả null); no-match/no-mapping → null; method missing/inactive → `FallbackConfigurationException` (fail loud §30); **zero-price = FallbackRate(0) hợp lệ**; label từ bridge config (không lộ Mageplaza title), estimate null; **gaps REPORT §R1** (Method::calculatePrice items-driven → glue Rate::calculatePrice + rule) **§R2** (isActive session-dependency — storefront consistent) **§R3** (DI mapping). 16 unit tests; full suite 1030/3643 (7 errors = Tracking baseline); compile + validator OK (0 finding NQT782). **0 vendor/Mageplaza/ShippingCore/carrier code edit.** Provider thật đầu tiên của fallback chain — E-SL2 decision giờ có thể hoạt động end-to-end khi Launchpad composition đăng ký mapping.
- **TASK-7AJ3K8 dev-complete (chờ TL Tier-2 review)** — GHTK alignment lên target architecture (Phase E-C1/GHTK, consumer runtime ĐẦU TIÊN của pipeline E): Secomm_Ghtk XOÁ private VN address resolution (`WardIdBridge` raw-SQL first-match / `BestEffortViVnResolver` locale tables / `DestinationAddressResolver` combine-4) → pipeline chuẩn: `GhtkDestinationResolver` Stage-2 adapter (alias `secomm_ghtk_address_map` qua reverse bridge → canonical `name_vi` fallback; **AMBIGUOUS = KHÔNG request — bỏ first-match, fail-closed intentional**; UNMAPPED + textual fallback = province name_vi + ward submit) + `GhtkAddressCapability`/`GhtkApiProfile` (D3 bundle GHTK_2025) + `GhtkTrackingFetcher`; client lên shared HTTP primitive (taxonomy 6 category + RetryExecutor; fee GET retry giữ nguyên, create-order SINGLE attempt, `GhtkApiException` surface giữ nguyên); tracking orchestration chuyển shared `TrackingReconciliationService` (GHTK còn thin shell + fetcher, webhook không đổi). ShippingCore: `RuntimeAddressContextBuilder` (scalar source + name-bridge fallback — fix F8 quote builder chỉ id-based) + manager AMBIGUOUS candidates passthrough + `handoffContext()` entry + `CarrierApiProfileInterface` (không default pref) + HTTP primitive + reconciliation service. VietNamAddress: `VnOperationalNameResolver` sibling contract (match name_vi/name_en trên reference layer — sửa latent bug match default_name=name_en; AMBIGUOUS candidates không pick). Legacy pickup config GIỮ NGUYÊN 100%; alias table giữ runtime key (re-key D6 = follow-up DB riêng). **DEC-TASK7AJ3K8-001 accepted (5 amendments, user acting SA/TL)**; scoped suite 508 tests pass (baseline 448; 7 errors full-suite = Secomm_Tracking pre-existing); grep gates sạch (0 directory knowledge trong Ghtk prod code; ShippingCore không import carrier). Evidence `.ai/evidence/TASK-7AJ3K8/evidence.md`.
- **TASK-STC3NB done (chờ TL review)** — ShippingCore COD payment identification (architecture v4 §4.1 delta, audit-driven): `Api\Cod\CodPaymentMethodResolverInterface::isCod(string): bool` + `ConfiguredCodPaymentMethodResolver` (config `secomm_shippingcore/cod/payment_methods` comma-separated, trim + exact case-sensitive match, empty/malformed → safe false, không default COD method) + system.xml/config.xml + DI preference; 8 unit tests; regression 388/1078 OK; compile + validator OK (0 finding STC3NB). **Audit vs architecture v4**: MATCH (rate semantics/service-level/fallback/handoff/resolver-seam/deps) · **PARTIAL DEFER** (capability per-operation + representations — interface đang per-carrier, implementers Ghn/Ghtk ngoài ShippingCore, cần task riêng Tier-2; CanonicalResolutionSnapshot persistence — DB Tier-2). 0 carrier/Mageplaza code. Kế tiếp: spec-first task capability per-operation + snapshot persistence khi GHN-C/GHTK-D consume thật.
- **TASK-Y3X6H5 done (chờ TL review) — ShippingCore RE-FROZEN** — v4 delta A–E implement (BC-safe reopen): **Delta A** `ShippingAddressOperation` (RATE/CREATE) + `AddressRepresentation` (UNIT_ID/TEXT_NAME) + `CarrierOperationAddressCapabilityInterface` per-op + `buildForOperation`/`handoffForOperation`/`handoffContextForOperation` + handoff `getSupportedRepresentations()`; old `CarrierAddressCapabilityInterface` @deprecated docblock-only (signature unchanged — GhnRateCalculator/GhtkAddressAdapter runtime không break); **Delta B/C** `CanonicalResolutionSnapshotInterface` + VO (canonical-only, failure_class NONE|AMBIGUOUS|UNMAPPED|TECHNICAL, provenance EXTERNAL ⇒ resolver; persistence DEFER Tier-2); **Delta D** resolver contract AMBIGUOUS-only explicit (docblock; 0 provider/0 invocation); **Delta E** COD verify-only (STC3NB). 14 unit tests mới (RATE-vs-CREATE scheme matrix + snapshot invariants + legacy regression 1:1); full suite 1249 tests (7 errors = Tracking baseline); compile + validator OK (0 finding Y3X6H5). **Consumer impact**: GhnAddressCapability/GhtkAddressCapability + GhnRateCalculator/GhtkAddressAdapter cần migrate sang per-op interface + handoffForOperation trong carrier-adaptation task kế tiếp (old path vẫn chạy đến khi migrate xong). **ShippingCore RE-FROZEN.**
- **TASK-5JQYMP done (chờ TL review)** — ShippingCore v5 fallback eligibility orchestration (DEC-FEATYA2C0W-005 + arch v5 §14/§15.1): `FallbackEligibilitySource` (TECHNICAL_FALLBACK | LEGACY_ADDRESS_FALLBACK — extending = amendment) + `FallbackEligibilityInterface`/VO (2 flags + isEligible + getSources deterministic) + `LegacyRateStrategy` (DIRECT_FALLBACK ≡ §15.1 "FALLBACK_ONLY" — naming documented, không collision bridge mode); aggregator/aggregate/orchestrator BC-safe optional eligibility param; eligible = aggregate technical OR legacy input; SUCCESS suppress giữ nguyên; provider at-most-once. Strategy resolver interface DEFER (consumer supply). 22 tests mới/updated; scoped 417/1157 OK; compile + validator OK (0 finding 5JQYMP). 0 carrier/Mageplaza edit. Kế tiếp: carrier adaptation (GHN/GHTK per-op capability + legacy strategy config) + GHN-C/GHTK-D + bridge mapping seed.
- **TASK-4ATBC4 (GHN-E2) Cancel + Return dev-complete, r2-aligned (chờ TL review)** — `GhnCancelService`/`GhnReturnService` + `GhnActionOutcome` VO + CLI `secomm:ghn:shipment:cancel|return`. Sandbox: cancel L8TAKR SUCCESS + repeat idempotent-positive; return trên cancelled → BUSINESS_REJECTED. Mutation taxonomy r2 (align TASK-GKHXY1): timeout/Remote/connect/5xx → UNKNOWN_RESULT (conservative — client không chứng minh before-send; 5xx để mở applied-vs-not); 429 → TECHNICAL_FAILURE (tách qua `ProviderRateLimitException` — definitively not-applied); auth/invalid → BUSINESS_REJECTED. KHÔNG order/tracking mutation; 0 schema change; admin UI defer E3 (đã đóng bởi TASK-PWHG0V). Ghn 273 tests; cross-module 959/0F/0E (số liệu tại thời điểm r2; E3 đưa lên 312/998).
- **TASK-WAWNDS (GHN RATE Type-5) PAUSED draft (chờ ShippingCore v10 frozen contracts)** — contract re-verified (docs+sandbox 2026-09-18): type-5 fee bắt buộc items[]; quantity=N ≠ N rows (per-unit serialization); aggregate >50kg + fee KHÔNG enforce 50kg/200cm; hard limit thật = 150cm/dim (SANDBOX, conflict docs 200 — sandbox thắng); weight per-parcel UNSETTLED (CREATE probes upstream-timeout) → không pre-reject; dims OMIT giữ (type-2 dims đổi giá). Estimator GHN-specific (PRODUCT_UNIT_AS_PACKAGE, transient, parent-parity expansion, decimal-qty fail-safe) + reason taxonomy mới (heavy-unsupported retired). v10: 0 GHN-local generic orchestration (grep-verified); FALLBACK_ONLY misuse + input-boundary dependency documented chờ frozen contract; buffer draft FLAG v10. Ghn 342/0F/0E; compile GREEN; validator 0 fail WAWNDS; 1 external failure (VietNamAddress concurrent stream). Evidence .ai/evidence/TASK-WAWNDS/type5-rate.md.
- **TASK-PWHG0V (GHN-E3) Magento Tracking/Admin + Label Boundary dev-complete (chờ TL review)** — E3-A: `getTracking()` qua `GhnTrackingResultBuilder` (1 provider query, CÙNG `GhnStatusMapper`, feed `ShipmentTrackingProcessor` §11, safe `Error` cho not-found/transport §10, curated display §7/§8 — log time từ `updated_date`), `isTrackingAvailable=true` sau runtime proof; KHÔNG tracking URL (donhang.ghn.vn soft-404, §9). E3-B: `POST /admin/secomm_ghn/shipment/cancel|returnShipment` (shipment_id-only input §36, ACL `Secomm_Ghn::shipment_actions→cancel_shipment|return_shipment`, message map §19, KHÔNG retry UI §20, visibility matrix §16 từ local state) delegate thẳng E2 services; `ShipmentReconciler` (fetcher→processor, non-fatal) sau SUCCESS — admin + CLI parity. E3-C: verdict **SUPPORTED_BUT_DEFERRED** — GHN có provider-hosted Print Order (docs 2026-09-16: `POST /shiip/public-api/v2/a5/gen-token` → `data.token` ~30' → print URLs A5/80x80/52x70; endpoint cũ `v2/shipping-order/print` không phải flow hiện hành) nhưng chưa adapt Magento shipment-label contract + invariant `secomm_physical` → `isShippingLabelsAvailable=false` BY DESIGN (locked), KHÔNG fake label §26; positive print-token runtime validation deferred đến khi có sandbox token hợp lệ + live order; label adapter = follow-up riêng. di.xml `CommandListInterface` registration fix (latent, lộ khi stale metadata bị xóa). Gates: Ghn **312**/0F/0E (+39); cross-module **998**/0F/0E; compile GREEN (sau cross-stream legacy logger fix + config.php module restore — incidents evidence §R); validator 0 fail stream này. N-Defect external: dev-store create mới fail CANONICAL_UNRESOLVED (address data drift, §R.3); sandbox token cần rotate (§R.6). Evidence `.ai/evidence/TASK-PWHG0V/e3-integration.md`. Backlog: GHN-F cutover, RATE type 5, currency boundary, COD policy, label adapter (nếu probe live order đạt).
- **TASK-GKHXY1 (GHN-E1) r2 dev-complete, gates GREEN (chờ TL review + closure)** — r2 corrections: security wording (shared-secret auth ≠ provider signature — GHN không sign callbacks, portal hỗ trợ merchant custom headers); dedupe identity OrderCode+Type+Time (ShippingCore shouldApply occurrence-aware — same status different Time = distinct event re-applied, exact resend no-op, null-occurredAt giữ legacy); taxonomy += LOST/DAMAGED terminal+commentable (lost→LOST, damage→DAMAGED, scrap→DAMAGED compat, delivery_fail giữ riêng); unsupported_event_type tách invalid_payload; scrap provenance = docs, flag re-verify. ShippingCore smallest shared delta (shouldApply + taxonomy — ShippingCore CHANGELOG 0.17.0; KHÔNG còn "0 edit" tuyệt đối). Runtime matrix PROVEN 10 case (repeat-status distinct, terminal sticky, stale skip, unsupported type, 401, unknown-order) — xem evidence r2. Pipeline: `GhnStatusMapper` (23 GHN statuses → 13 normalized; exception→UNKNOWN), webhook `POST /secomm_ghn/webhook/tracking` (secret header hash_equals fail-closed 401; Type-aware: chỉ switch_status; raw sanitized bỏ PII), `GhnTrackingFetcher` + `TrackingRefreshService` + cron 30' opt-in (cùng mapper — §30 một nguồn sự thật). KHÔNG order business mutation (grep gate). `isTrackingAvailable`/`getTracking()` defer E3. Gates r2 final: compile 0 lỗi stream này (2 lỗi external pre-existing legacy `Secomm_GiaoHangNhanh` — stream khác working-tree, ghi nhận N-Defect); validator 0 fail stream này (32 FAIL pre-existing records stream khác); 6 PHPUnit deprecations pre-existing module khác. Evidence `.ai/evidence/TASK-GKHXY1/`. Ghn 273 tests; cross-module 959/0F/0E.
- **TASK-9Q5ZAK (GHN-D) r3 dev-complete (chờ TL review + closure)** — r2 physical layer + r3 correction sandbox-driven: type-5 payload = root `weight` (factual Σ — PROVIDER-MANDATORY, probe 400 'required' tag) + `items[]` per physical package; root dims OMITTED (probe ACCEPTED không dims); synthetic aggregate XÓA (largest-by-volume/Σ-dims); Σ>50,000g accepted với items[] (L8TTRG 550,000 VND). Persister MERGE semantics (forward-compat native label flow — audit: LabelGenerator writer duy nhất, GHN FALSE 0 collision; verdict COMPATIBLE_WITH_CONVERSION, GHN-E boundary phải merge). markSubmitted clear reason. Cross-module 862 tests 0F/0E. — ShippingCore physical facts layer mới (DEC-TASK9Q5ZAK-001 proposed): `Api/Physical/` + `Model/Physical/` (Persister trên `sales_shipment.packages` marker `secomm_physical`; StoreWeightConverter moved; ConfiguredDefaultPackageDimensions prefill-only `secomm_shippingcore/physical/*`). GHN `GhnPhysicalParcelInterpreter`: 1 pkg <20,000g → type 2 root; ≥20,000g/multi-pkg → type 5 items[] 1-item-per-package; limits fail-closed. **Type-5 CREATE + multi-package SUPPORTED**. Admin "Package Information" (prefill + editable + Add Package + limits); POST = authoritative, snapshot = retry, thiếu → fail-closed. Observer in-flight guard. parcel_* config REMOVED. Sandbox: type-2 L8TWX8 · type-5 L8TWAF 550,000 · multi L8TWAP 550,000 (root Σ biên OK) · invalid 0 HTTP — cancelled sạch. Cross-module 859 tests 0F/0E.
- **TASK-9Q5ZAK (GHN-D) CREATE-only slice r1 (history — superseded bởi r2 cho parcel source)** — GHN-C foundation + U1 fixed (BUG-ZTGGYZ). Trigger: observer `sales_order_shipment_save_commit_after` → `GhnShipmentCreationService` (PENDING anchor trước API; `client_order_code = GHNS{shipment id}`; SUBMITTED short-circuit; UNKNOWN cho uncertain — không auto-retry) → CREATE handoff (`VN_ADMIN_2025` source==target EXACT) → Stage-2 verbatim names → POST `v2/shipping-order/create` → native Track (dedupe). Bảng `secomm_ghn_shipment` (Tier-2 approved). Parcel dims = config merchant (empty/>200 fail-closed INVALID_PARCEL); type-5 fail-closed 0 HTTP; `cod_amount` không gửi (0 payment inspection); `payment_type`/`required_note` validate enum. **Sandbox**: create thật L8TKYG 122,100 VND merged-ward "Xã Tân Thanh" (DEC gate #4 evidence); provider idempotency same-code→same-order PROVEN; recovery CLI `secomm:ghn:shipment:retry`. Ghn 203 tests 0F/0E; gates 0. Deviations: CREATE-only (cancel/return/MQ → GHN-E/F); synchronous thay MQ. Evidence `.ai/evidence/TASK-9Q5ZAK/create-slice.md`.
- **TASK-FMBBSD (GHN-C) QC L3 r3 — REAL QUOTE PATH PASS (chờ TL closure)** — U1 đã fix ở VietNamAddress (BUG-ZTGGYZ: `UnitSnapshotWriter` synthesize province edge parent_code=region_code cho L2 + `HierarchyParentBackfill` + CLI `secomm:vietnam-address:hierarchy:repair`; DB repair 3,321+696 rows, idempotent, counts nguyên vẹn). **QC CASE 1 rerun: method `secomm_ghn` hiển thị price 214,500 VND (fee sandbox thật) qua REAL Quote → collectShippingRates → carrier → ShippingCore v5 → PRE_2025 → Stage-2**; snapshot sạch GHN identity; Kỳ Lừa merged ward vẫn fail-closed; **U2 = NOT REQUIRED** (name-only pipeline SUCCESS — không mở ShippingCore task). Regression: Ghn+Ghtk+VietNamAddress+ShippingCore 802 tests 0F/0E. Follow-up riêng: `unitFile(PRE_2025)` stale reference file. Evidence `.ai/evidence/TASK-FMBBSD/qc-l3-checkout.md` §8.
- **TASK-5XQXZK dev-complete (chờ TL/SA review)** — FEAT-GGTWXW Mageplaza TableRate fallback composition (DEC-TASK5XQXZK-001 accepted 2026-09-15, user acting SA/TL; 22-point directive): **Magento = carrier orchestrator** (KHÔNG fan-out registry — REJECT CarrierRateProviderInterface); ShippingCore `CarrierRateOutcomeCollectorInterface` (pair carrier_code+method_code, bracket per collectRates execution, orphan-safe, không persist) + `FallbackEligibilityPolicyInterface`/SafeDegradationEligibilityPolicy (TECHNICAL_FAILURE + CANONICAL_AMBIGUOUS eligible; UNMAPPED/unsupported/config NOT; không parse message) + reasons mới CANONICAL_AMBIGUOUS/CANONICAL_UNMAPPED/INVALID_CONFIGURATION; GHN/GHTK report outcomes (GHN classify AMBIGUOUS/UNMAPPED từ handoff candidate count); Launchpad bridge: per-method `show_to_customer`/`use_as_fallback` + membership pair-identity (3 extension tables FK CASCADE + whitelist, ĐÃ apply local DB), outer seam plugin `around Shipping::collectRates()` (visibility filter + FallbackCoordinator per-group gate: non-participating ≠ failure, SUCCESS suppress, policy-eligible → internal price append), City/Area dimension stable `city_code` + 3-tier precedence chỉ narrow location scope (zero-regression guard + unresolved không match + SUM/MIN/MAX giữ nguyên trong tier), admin Launchpad Settings tab (preference Tabs), member options dynamic (mptablerate excluded), rate form/grid/export city_code, CSV importer preference subclass superset columns + round-trip fix + unknown-code fail-loud; **deprecate global mode + methodMapping/labels + guard plugin mode-based + CarrierServiceLevelInterface (arch doc §12)**; FallbackRateRequestInterface KHÔNG đổi (runtime context đủ — directive §14). Architecture doc v7 revision note + §9/§12/§15.1/§16/§17/§18/§29 amended. Tests: ShippingCore 267 OK, Launchpad 32 OK, Ghn 192 OK, Ghtk 237 OK (1 deprecation pre-existing); compile OK; 3 tables verified DESCRIBE; validator: 0 finding mới (23 FAIL pre-existing khác task). **Surgical fix pre-existing**: Secomm/Ghn/etc/di.xml virtualType lồng sai trong CommandList arguments (GHN-E1) — di chuyển ra top-level, zero behavior. Evidence `.ai/evidence/TASK-5XQXZK/evidence.md`. Follow-up backlog: integration E2E với method data thật (DB còn trống), Ahamove adopt collector khi lên P2, session-dependency limitation Method::isActive cho REST/GraphQL (đã documented, giữ nguyên).
- **ARCH v9 amended (2026-09-18, chờ TL/SA acceptance — DEC-FEATYA2C0W-006)** — Carrier Eligibility/Destination Scope (ALL|SELECTED_ZONES) + Canonical Zones (provider-neutral, canonical codes, composition-owned definitions) + RateSourceMode (CARRIER_ONLY/CARRIER_WITH_FALLBACK/FALLBACK_ONLY) + AddressResolutionPolicy (STRICT/FALLBACK; PICK_PRIMARY DEFERRED) + Fallback Eligibility normalization (technical/mapping-missing/capability-unsupported YES · auth-config configurable+mandatory warning · business rejection NO) + **LegacyRateStrategy FULLY SUPERSEDED** (config axes → RateSourceMode + AddressResolutionPolicy; DIRECT_FALLBACK ≡ FALLBACK_ONLY, MAP_THEN_FALLBACK ≡ CARRIER_WITH_FALLBACK+FALLBACK policy; constants deprecated alias). Implementation = controlled task riêng SAU khi v9 accepted (chưa code). Doc-only — 0 production change.
- **ShippingCore v10 freeze verification (2026-09-18) → NOT FROZEN** — verify ownership + wiring: LegacyRateStrategy chỉ còn constants (0 orchestration consumer) ✓; bridge policy-neutral ✓; selector thuộc VietNamAddress ✓; snapshot/provenance ownership ShippingCore ✓; candidate-order invariant ✓. **2 blockers chưa wire**: (1) `RateSourceMode` chưa có orchestration consumer (FALLBACK_ONLY short-circuit flow chưa implement — contracts chỉ); (2) `AddressResolutionPolicy` evaluation chưa wire vào handoff/decision flow. Blocker external: `Secomm_Ghn/etc/adminhtml/system.xml` line 116 compile XML invalid (stream GHN khác — không thuộc ShippingCore). Kế tiếp: spec-first implement RateSourceMode orchestration + AddressResolutionPolicy evaluation → RE-FROZEN → GHN/GHTK adaptation.
- **TASK-8MQHJX Phase A+B+C done (chờ TL review)** — ShippingCore v10 runtime eligibility + execution gating: Phase A `DestinationScope` (ALL|SELECTED_ZONES) + `CanonicalZoneInterface`/`CanonicalZone` VO + `CanonicalZoneRegistryInterface`/`CanonicalZoneRegistry` (0-zone valid; duplicate/empty fail-fast); Phase B `CanonicalZoneMatcherInterface`/`CanonicalZoneMatcher` (deterministic fail-closed: enabled → province → include ward → exclude ward; exclude wins; empty includeWards = no ward restriction, null ward fail gate) + `CarrierEligibilityResultInterface`/`CarrierEligibilityResult` (+ static factories `eligible`/`ineligible`) + `CarrierEligibilityEvaluatorInterface`/`CarrierEligibilityEvaluator` (ALL short-circuit không đụng registry; SELECTED_ZONES first-match-wins theo config order, skip unknown/disabled zone; unknown scope fail-closed). Phase C `CarrierRateExecutionServiceInterface`/`CarrierRateExecutionService` (hard order: eligibility → mode → origin readiness → AddressResolutionPolicy qua handoff service → realtime contributor → contribution; ineligible = 0 realtime + 0 fallback; FALLBACK_ONLY skip origin/policy/realtime, emit LEGACY_ADDRESS_FALLBACK; realtime mode + origin unresolved fail-closed KHÔNG re-mode; STRICT block → no realtime no fallback; FALLBACK+AMBIGUOUS → no realtime, legacy eligibility chỉ CARRIER_WITH_FALLBACK; PICK_PRIMARY fail block như STRICT, success = plain resolved handoff — không candidates/order/rank qua boundary; UNMAPPED textual vẫn tới realtime) + `CarrierRateExecutionDecision` (compose outcome+handoff+eligibility+reason, guard realtime↔outcome) + `CarrierRateExecutionRequest` (validate scope/mode/policy fail-fast) + `RealtimeCarrierRateContributorInterface` (carrier-owned, không default). DI: matcher/registry/evaluator/request/decision/service preferences + `canonicalZones` item entries. **Amendment TL-approved (2026-09-18, đóng representational gap — không tạo v11)**: `FallbackEligibilitySource.INTEGRATION_LIMITATION` (source thứ 3 frozen; §35.5 đã named từ Rev v9 nhưng contract 2 nguồn không represent được) + `FallbackEligibility::integrationLimitation()`/`hasIntegrationLimitationEligibility()`; UNAVAILABLE+PROVIDER_MAPPING_MISSING + CARRIER_WITH_FALLBACK → INTEGRATION_LIMITATION (outcome vẫn UNAVAILABLE, không reclassify); `INVALID_CONFIGURATION` merchant-side giữ fail-closed kể cả khi policy opt-in (§35.5 auth/config configurable seam cần constant provider-auth riêng + warning seam — ambiguity hẹp ghi nhận Phase D); SERVICE_UNAVAILABLE/business rejection không bao giờ emit INTEGRATION_LIMITATION. Fix bug thật matcher: biến `$includeWards`/`$excludeWards` dùng sai tên (undefined → fail mọi zone có include ward). **TASK-8MQHJX CLOSED (Phase D done 2026-09-18) — ShippingCore v10 runtime orchestration = COMPLETE.** Phase D: E2E integration `CarrierRateExecutionFlowIntegrationTest` (15 tests/73 assertions, REAL chain evaluator→mode→origin→handoff/VietNamAddress→contributor→aggregator→orchestrator; case matrix §5 + address policies + multi-carrier realtime-success suppression: provider mock là seam dispatch DUY NHẤT thuộc orchestrator); 2 defect fix: (1) orchestrator đọc thêm `hasIntegrationLimitationEligibility()` — §35.5 mapping-missing fallback giờ dispatch được E2E, ownership không đổi; (2) `FallbackEligibility::__set_state()` — DI compiler var_export object default làm CLI fatal bootstrap, smoke-check phát hiện. Cross-module regression: ShippingCore 359/980 OK · VietNamAddress 182/510 OK · Ghn 333/210823 OK · Ghtk 237/585 OK (1 deprecation pre-existing); compile OK; validator exit 0. Invariant grep: 0 active violations (zone/eligibility/is_primary/Mageplaza-call/fallback-dispatch/reason-parsing/LegacyRateStrategy consumer đều 0; hits còn lại là docblock/VO-field false positive). Runtime smoke BLOCKED_BY_ENVIRONMENT (sandbox credentials có nhưng origin_district_id unset + catalog trống). Provider-auth/config seam: DEFERRED (không consumer runtime; ghi nhận SSOT). Records: ticket `.ai/tickets/TASK-8MQHJX-...` + evidence `phase-a/phase-d.md` + SPEC CLOSED + SSOT v10 Phase D completion block (không tạo v11). Follow-up backlog trong ticket: provider-auth warning seam, quote smoke store thật, FulfillmentCore baseline (external), GHN/GHTK adoption khi composition integration. Phase C = **COMPLETE** sau amendment. Tests ShippingCore scoped 344/907 OK (Phase B 5 files + Phase C 2 files + eligibility-source amendment tests: FallbackEligibilityTest +2, service tests +5 thay gap test); compile OK; validator exit 0; §25 regression (matcher fix + factories) permanent trong suite. Architecture SSOT v10 §35.5 + amendment note (không tạo v11). Next = Phase D: integration + cross-module regression + architecture closure — KHÔNG tự bắt đầu, chờ TL.
- **FEAT-HEHJQ4** (VNPAY) gated at TL/SA plan approval (DEC-FEATHEHJQ4-001..006 `proposed`) — không đổi.

<!--
DURABLE snapshot. Update via update-project-state when a record → Done.
No session scratch here — session state → .ai/runtime/session/ (Phase 1d).
-->
