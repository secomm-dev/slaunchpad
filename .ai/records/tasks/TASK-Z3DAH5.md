---
id: TASK-Z3DAH5
type: task
title: '[LA-01] Quick View — Hyvä modal lazy-load product essentials + ATC từ PLP'
project_code: SLP
parent: null
external_refs:
  tickets: SLP-157 (LA-01)
legacy_ids: []
mode: B
specification_level: MINI
spec_status: VALID
specification_ref: Embedded Mini-Spec
risk: medium
status: in_review
created: 2026-09-15
updated: 2026-09-15
ticket_ref:
affects_version: Magento 2.4.8-p5 + Hyvä 3.x (default theme 1.5.2)
decisions: []
decision_assessment: minor — architecture decided by user directive (Hyvä UI base, KHÔNG dùng ajax-atc plugin); ATC path = fetch POST checkout/cart/add (session-aware) sau khi loại GraphQL vì session-quote isolation
components:
  - app/code/Launchpad/QuickView
  - app/design/frontend/Secomm/launchpad/Magento_Theme
  - app/design/frontend/Secomm/launchpad/Magento_Catalog
  - app/design/frontend/Secomm/launchpad/i18n
source_areas:
  - hyva-ui-modal
  - graphql-products-query
  - checkout-cart-add-ajax-contract
  - theme-i18n
changes_project_state: true
changes_architecture: false
changes_integration: false
changes_known_limitations: false
verified_against_commit:
last_verified: 2026-09-15
supersedes: []
---

# [SLP][TASK-Z3DAH5] [LA-01] Quick View — Hyvä modal lazy-load product essentials + ATC từ PLP

<!-- External ticket: SLP-157 (LA-01). Scope: Quick-view trigger trên product card + modal lazy-load (image/name/price/options/qty/ATC) + configurable state/validation, không reload PLP, mini-cart cập nhật sau ATC. Không bao gồm: full PDP content, reviews/storytelling, bundle/custom options phức tạp. -->
<!-- Generated by Claude Code per task-record-template (Mode B). -->

## Summary

Thêm Quick View cho PLP: trigger trên product card → modal Hyvä (native `<dialog>` + `x-htmldialog`, theo pattern Hyvä UI `modal.A-simple`) lazy-load product data bằng GraphQL `products(query)` → render name/gallery/price/options/qty + ATC qua fetch POST `checkout/cart/add` (AJAX contract core, header `X-Requested-With`) → success dispatch `reload-customer-section-data` → mini-cart drawer (SLP-158) cập nhật, modal hiển thị confirm state.

**Quyết định kiến trúc theo chỉ thị user (2026-09-15):**
- **Apply Hyvä UI** — modal container theo pattern `modal.A-simple` (native `<dialog>` + Alpine), gallery/swatch dùng markup idiom Hyvä UI. KHÔNG copy plugin `ajax-atc.A-simple` (user: "không cần ajax cart" — không intercept form toàn trang).
- **ATC path = fetch POST `checkout/cart/add`, KHÔNG GraphQL `addProductsToCart`**: phân tích loại GraphQL vì section data cart KHÔNG expose `cartId` (`vendor/magento/module-checkout/CustomerData/Cart.php` — grep 0) và masked quote id của session PHP không lấy được từ FE → mutation sẽ tạo quote mới ngoài session → cart page PHP + Mageplaza OSC checkout MẤT ĐỒNG BỘ. Fetch POST tới `checkout/cart/add` giữ session quote, form-key, store cookie — contract AJAX JSON có sẵn trong core (`Controller/Cart/Add.php::goBack()` line 211-227: `isAjax()` → JSON `{backUrl}` khi fail, JSON không `backUrl` khi success).

**Findings/Traps đã verify trước khi code:**
- Card PLP Hyvä đã có form POST ATC class `product_addtocart_form` (`Magento_Catalog/templates/product/list/item.phtml:80-93`, verbatim vendor) — Quick View KHÔNG đụng form này; ATC card giữ full-page POST như cũ.
- Event `reload-customer-section-data` là cơ chế refresh mini-cart drawer của SLP-158 (`Magento_Theme/templates/html/cart/cart-drawer.phtml:59`) — modal chỉ dispatch, không sửa drawer.
- Tailwind v4 pitfall `scale: 0` dialog (TASK-7P5RJP) — modal Quick View dùng native `<dialog>` + `x-htmldialog` (KHÔNG dùng dialog class `wrap-modal-*` của SocialLogin) → tránh bẫy, nhưng vẫn phải verify render đầu tiên.
- Store context: fetch POST append `___store` từ `location.search` (pattern đã chứng minh SLP-158 — cookie store không set được qua `?___store=` local); GraphQL query gửi header `Store` từ view model `Hyva\Theme\ViewModel\Store::getStoreCode()`.
- Contract AJAX `checkout/cart/add`: fail configurable → JSON `{backUrl}` (navigate); success → JSON khác (không `backUrl`). Response HTTP error → fallback form submit thuần.
- Dynamic Tailwind classes: toàn bộ class dùng trong modal là static strings trong `.phtml` — scanner `@source "../../**/*.phtml"` thấy hết (`tailwind-source.css:19`), không class sinh runtime.
- CSV: 3 key mới/file — "Quick view", "Choose an Option..." (key core configurable.phtml:65, theme dict chưa có), "There was a problem adding your item to the cart." (mirror ajax-atc). Key còn lại reuse dict sẵn có ("Add to Cart", "Qty", "In stock", "Out of stock", "You added %1 to your shopping cart.", "As low as").

## Verify Results (2026-09-15)

**Playwright 24/24 PASS** (`.ai/evidence/TASK-Z3DAH5/playwright-verify.txt`) — AC-001 (no reload, cả vi/en), AC-002 (configurable guard + selection + ATC, cart count 1→2), AC-003 (mini-cart `summary_count` cập nhật qua `reload-customer-section-data`), AC-004 (Esc/focus/mobile 375 no h-scroll/dialog fit). Regression ATC card form POST cũ PASS. Console 0 non-ambient error. Dictionary 10/10 PASS (vi framework-level + en file identity, `verify-i18n.php`). Screenshots 3 (desktop vi configurable / mobile 375 / en).

## Traps & Findings (implement + verify)

1. **Alpine MutationObserver OFF trên theme này** (Hyvä theme module 1.5.2 + Alpine 3.14.3): node có `x-data` thuần render server-side trong product list KHÔNG được auto-init (chỉ `[x-data][x-defer]` được init qua defer plugin `view/base/templates/page/js/plugins/v3/`); node clone/inject sau start không bao giờ init; **`<template x-if>` mount content mà directives KHÔNG bind** (form submit native xuyên qua). → idiom bắt buộc trên theme: `x-data` + `x-defer="intersect"` cho component trong card; modal dùng `x-show` + optional chaining (`product?.name`) cho mọi directive chiếu tới data lazy-load. Đề xuất LL.
2. **GraphQL `products(filter:{sku:{eq}})` với sku child variant trả về PARENT** (configurable/grouped) — user báo "lấy nhầm data" → **ĐÃ FIX (09-16)**: query `pageSize: 10` —Magento expansion trả cả parent LẪN child trong results (verify: `in`-filter 3 child skus → 5 items) → client-side `items.find(i => i.sku === sku) || items[0]` → modal luôn mở đúng sản phẩm card; child variant ATC standalone hoạt động (Terra Mug count 0→1 + drawer). Verify `qv-exact-sku.js` 4/4 PASS. Fixture standalone simple thật: `table-runner-linen`, `table-ovale` (configurable parent).
3. **ENV đã bị session/team khác thay đổi giữa chừng** (2026-09-15): (a) theme storefront switched `Secomm/launchpad` (id 5) → `Hyva/default` (id 4) ở cả 3 scopes DB `core_config_data` — user chỉ thị switch lại về launchpad và GIỮ (không restore về Hyva/default); (b) `config.php` working tree: `Snowdog_Menu => 0` + reorder modules (chưa commit, của người khác — không đụng). Cần TL ghi nhận việc theme change không có DEC/memory.
4. Tầng page-cache theo URL trả stale markup sau template edit (cache-buster `?cb=` bypass được) — `cache:flush` toàn bộ là bước bắt buộc sau khi sửa template/block, `cache:clean block_html` không đủ.
5. `checkout/cart/add` AJAX contract: `X-Requested-With` → JSON `{backUrl}` khi fail, JSON không `backUrl` khi success (`Controller/Cart/Add.php::goBack()` line 211–227); response `redirected` → navigate. Cả 3 fallback đã handle trong `addToCart()`.
6. CSV ownership: `i18n/*.csv` bị root-owned sau session khác → append as secomm bị Permission denied → chown lại `secomm:secomm` (pattern BUG-SDZPCD).
7. **Incident env 09-15 ~11:37–11:45 (user report "bấm không thấy quickview hiện lên" — root cause KHÔNG phải Quick View code)**: session khác chạy `composer require monsoonconsulting/magento2-hyva-ajax-add-to-cart` (package 3rd-party AJAX ATC) rồi revert `composer.json`/`composer.lock` về HEAD mà không regen autoload → `vendor/composer/installed.json` còn sót monsoon nhưng dir package thiếu → **autoload fatal → storefront 500 TOÀN CỤC**; kèm DB mất `setup_module` version Launchpad_QuickView (500 "Please upgrade your database"). Khôi phục: `composer install` (sync theo lock HEAD, offline) + `setup:upgrade` + `cache:flush` → HTTP 200. **TL lưu ý: 2 phiên composer/config đụng nhau trong ngày — cần quy ước khóa composer trong giờ làm việc song song.**
8. **Trigger Quick View = native `onclick`** (không `@click` Alpine + không `x-data`/`x-defer`): loại trừ hẳn dependency Alpine-init timing (case bấm ngay khi card vừa hiện chưa qua intersect-init). `onclick` là idiom có sẵn trong Hyvä default theme (`Magento_SendFriend`, `Magento_Customer sign-in`) + theme overrides (SocialLogin). Verify user-behavior 3/3 PASS (desktop card đầu / card sâu sau scroll / mobile 375 — click ngay không chờ).

## Scope Extension: Admin enable/disable config (2026-09-15, cùng ngày)

**Module mới `Launchpad_QuickView`** (app/code/Launchpad/QuickView — config + view model only, không schema/cron):
- `system.xml`: **section `hyva_theme_quickview` trong tab `hyva_themes`** (Stores → Configuration → Hyvä Themes → Quick View — chỉ thị user "không để config trong Secomm"), field `hyva_theme_quickview/general/enabled` (Yes/No, **default Yes**, `canRestore`, scope default/website/store — vi/en bật tắt độc lập).
- `config.xml` default enabled=1; `ViewModel\Config::isEnabled()` (`ScopeConfigInterface::isSetFlag`, SCOPE_STORE); README + CHANGELOG.
- Wiring: theme `item.phtml` guard trigger qua `$viewModels->require(Config::class)`; 2 layout files thêm `ifconfig="quickview/general/enabled"` trên block `quickview.modal` (disabled → block không render hẳn).

**Verify config**: enabled default → 12 triggers + modal (vi); `config:set ... 0` store vi + `cache:flush` → **0 trigger + 0 modal** trong khi store EN vẫn 12 + 2 (per-store isolation PASS); bật lại → Playwright smoke e2e PASS (mở modal + ATC success sau toggle). **Yêu cầu env**: module:enable → `setup:di:compile` → **`setup:upgrade` bắt buộc** (không chạy = 500 "Please upgrade your database: Launchpad_QuickView schema: none, required 1.0.0") → cache:flush. **ENV note**: sau verify còn 2 rows `core_config_data` stores/default — `quickview/general/enabled = 0` (**orphan** — path cũ trước khi dời sang tab Hyvä, không còn code nào đọc) + `hyva_theme_quickview/general/enabled = 1` (trùng default; as-found là no-row). Behavior không đổi; QC/TL DELETE cả 2 rows sau QC nếu muốn sạch tuyệt đối (`DELETE FROM core_config_data WHERE path IN ('quickview/general/enabled','hyva_theme_quickview/general/enabled') AND scope='stores' AND scope_id=1`).

## Scope Extension 2: Tương thích Monsoon_HyvaAjaxAddToCart (2026-09-15, user chốt)

**Quyết định**: storefront dùng `monsoonconsulting/magento2-hyva-ajax-add-to-cart` (session khác deploy **source-committed tại `app/code/Monsoon/HyvaAjaxAddToCart`** — pattern Mageplaza; bản composer của phiên này đã `composer remove` để tránh double-definition autoload error). Quick View phải tương thích.

**Tương thích đạt được (không sửa Monsoon/vendor):**
1. **Modal form Quick View không match selectors Monsoon** (`.product_addtocart_form, #product_addtocart_form`) → không bao giờ double-intercept; ATC modal vẫn qua fetch riêng.
2. **Card ATC → Monsoon AJAX** (T1: no-nav, count +1, `product-addtocart-success` fired).
3. **Handover UX theo config `checkout/options/ajax_cart_open_after_add_to_cart`**: =1 → sau ATC từ Quick View: modal đóng + dispatch `toggle-cart` mở drawer (nhất quán card/PDP). `Launchpad_QuickView\ViewModel\Config::isOpenCartAfterAddToCart()` đọc path đó (không hard-dependency Monsoon module).
4. **PDP KHÔNG cho Monsoon intercept** — `checkout/options/ajax_add_to_cart_selectors = ".product_addtocart_form"` (bỏ `#product_addtocart_form`): root cause double-submit trên PDP — `hyva.formValidation.onSubmit` (theme product-form.phtml) **luôn preventDefault + `form.submit()` khi valid**, song song fetch Monsoon → 2 POSTs, count ×2, navigation. PDP giữ native POST (như trước Monsoon; TASK-QX93G3 đã ghi "PDP ATC là full-page POST"). Nếu muốn PDP AJAX sau này → ticket riêng (phải đồng bộ onSubmit với AJAX).

**Verify**: Playwright T1 card Monsoon AJAX PASS; T2 Quick View +1-only + modal-closes + drawer-opens PASS; T3 PDP native POST single-add PASS (`monsoon-compat.txt`); Quick View full suite **23/23 PASS** với Monsoon active (handover asserts updated).

**Cấu hình Monsoon hiện tại (local)**: `enable_ajax_add_to_cart=1`, `ajax_cart_open_after_add_to_cart=1`, `ajax_add_to_cart_selectors=".product_addtocart_form"`, delay default 1000.

## Scope Extension 3: Swatch kiểu PDP + description/information + homepage widget (2026-09-16, user)

1. **Configurable options render như PDP** — thay `<select>` bằng swatch radio theo pattern `Magento_Swatches::product/view/renderer.phtml` + `swatch-item.phtml` (label `.swatch-option` + `data-swatch-type` text/color/image + input `opacity-0 size-full m-0`); GraphQL thêm `swatch_data { value }` (hex → color, URL → image, khác → text); legend hiển thị `Label: <selected>`; option hết hàng disable (`opacity-40 pointer-events-none`). CSS `.swatch-option` có sẵn trong bundle.
2. **Thông tin sản phẩm trong modal** — SKU (`__('SKU')`), short description (`x-html`), **"More Information"** + description HTML (giống nguồn nội dung PDP); query thêm `sku`, `short_description`, `description`.
3. **Homepage widget** — modal block chưa có trên CMS pages (trigger render nhưng không có listener) → theme layout mới `Magento_Cms/layout/cms_index_index.xml` + `cms_page_view.xml` (cùng block `ifconfig`).
3b. **Related/Upsell/Cross-sell** (user: "chưa work với các widget relative, upsell cross sell") — PDP related slider render trigger nhưng không có modal → theme layout mới `Magento_Catalog/layout/catalog_product_view.xml` + `Magento_Checkout/layout/checkout_cart_index.xml` (cùng block). Verify `sliders-verify.txt` 3/3: PDP related "Overnight Duffle" mở đúng + ATC; cart crosssell "Set of Sprite Yoga Straps" mở đúng (fixture Joust có related/crosssell links; upsell dùng cùng slider template — cùng cơ chế).

**Fix visual 09-16 (user feedback "color không giống default + mở rộng width")**: `data-swatch-type="visual"` (không phải "color") để khớp CSS `.swatch-option[data-swatch-type=visual]`; visual span `class="block"` **30×30** (khớp card — `SwatchRenderer::getSwatchDimension` default 30; label 38×38 tròn đầy màu) + `background-color` inline / `<img>` cho image swatch; modal width `w-[min(92vw,44rem)]` → **`w-[min(92vw,60rem)]`** (960px) — class mới cần `npm run build` + **cp tay styles.css sang `pub/static/frontend/Secomm/launchpad/{vi_VN,en_US}/css/`** (gotcha F3 TASK-7P5RJP — static deploy quick bỏ qua styles.css ở mode default).

**Verify v2**: Playwright v2 suite **8/8** (home widget mở modal + SKU + description; swatch radio 0-select; legend selected; color+text swatch `sofa-meridian` 3 màu/4 text; ATC handover drawer ×2; console sạch) + main suite **20/20** (T5 live EN SKIP — store-switch local hỏng pre-existing, cookie lẫn `?___store` đều không switch; en identity đã verify dict 10/10 + live trước đây) + compat **4/4**. Screenshots `qv-v2-swatch-info.png` (swatch + SKU + Thông tin thêm), `qv-v2-homepage.png`.

**ENV note 09-16**: (a) `hyva_theme_quickview/general/enabled` default scope bị ai đó set 0 → EN store tắt; đã set lại 1; (b) store-switch local (`?___store` + cookie `store`) không hoạt động — pre-existing, không thuộc change set; (c) transient GraphQL pending (server busy session khác) — suite cần retry-pick chống flake.

## Mini Spec

**Context**: Fashion storefront vi_VN/en_US; PLP card Hyvä verbatim vendor; mini-cart drawer SLP-58 (QX93G3) nghe `reload-customer-section-data`; checkout = Mageplaza OSC đọc session PHP quote — mọi đường ATC phải giữ session quote.

**AC** (từ ticket SLP-157, đo được):
1. AC-001 Không reload PLP: mở modal + chọn option + ATC thành công — URL/document không thay đổi.
2. AC-002 Configurable selection/price/stock đúng: chọn đủ option → giá = giá variant, out-of-stock variant disable đúng; thiếu selection → ATC chặn, hiện message.
3. AC-003 ATC thành công cập nhật mini-cart: dispatch `reload-customer-section-data` → drawer/badge số lượng cập nhật; item có đúng option + qty trong quote (verify cart API).
4. AC-004 Modal accessible/mobile-ready: Esc + backdrop close, focus trap native `<dialog>`, `aria-*` đúng, hiển thị tốt @375px.

**Scope**: simple + configurable product. **Out of scope**: full PDP content, reviews, bundle/grouped (giữ nút Configure gốc), custom options (basic → follow-up nếu PM cần), auto-open drawer sau ATC (follow-up SLP-158 đã ghi), gallery slider/zoom nâng cao (main image + thumbnails đơn giản).

**Approach** (5 bước):
1. Override `Magento_Catalog/templates/product/list/item.phtml` (verbatim vendor + 1 button Quick View trong cụm icons, dispatch window event `open-quickview` `{sku}`).
2. Block `quickview.modal` template `Magento_Theme::html/quickview/modal.phtml` — layout `catalog_category_view` + `catalogsearch_result_index`; modal `<dialog>` + Alpine `initQuickView` nghe event.
3. Lazy-load: fetch `/graphql` `{products(filter:{sku:{eq}})}` (media_gallery, price_range, price_tiers, stock_status, configurable_options + variants) + header `Store`; cache per sku.
4. Configurable state: `<select>` per option, matrix disable theo variants khớp selection + stock; price/stock update khi đủ selection; validate chặn ATC thiếu option.
5. ATC: fetch POST `checkout/cart/add` (FormData form + `X-Requested-With`) → JSON: success → dispatch reload + confirm state; `backUrl` → navigate; error → message modal. i18n +3 key/file; Tailwind build; cache flush as secomm.

**Verification plan**: Playwright vi/en 2 store + mobile 375: AC-001..004 + regression card ATC (form POST cũ không vỡ) + drawer refresh + GraphQL payload inspect; dictionary verify framework-level; evidence `.ai/evidence/TASK-Z3DAH5/`.

**Risks**: modal render pitfall (verify đầu), store mismatch khi `?___store=` (append param), configurable edge (variant out-of-stock giữa 2 option), section reload hiển thị flash message server song song modal confirm (chấp nhận, chuẩn Magento AJAX cart).

**Estimate**: 14h–20h.
