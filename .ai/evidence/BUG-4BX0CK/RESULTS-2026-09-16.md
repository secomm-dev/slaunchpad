# BUG-4BX0CK / SLP-234 — Implementation Evidence (2026-09-16)

VietQR amount not converted to VND on non-VND store views. Fix implemented, awaiting TL review (Tier 2 — payment logic) + L3 QC.

## Changed files

- `app/code/Secomm/VietQr/Model/VndAmount.php` (new) — VND amount resolver: order currency VND → `grand_total`; base currency VND → `base_grand_total`; otherwise convert via directory rate (throws when no rate — fail closed)
- `app/code/Secomm/VietQr/Model/VietQr/RequestBuilder.php` — QR API payload amount via helper
- `app/code/Secomm/VietQr/Observer/GenerateQrAfterOrder.php` — `vietqr_amount` snapshot via helper
- `app/code/Secomm/VietQr/Block/PaymentInfo.php` — retry path, `getAmount()` fallback, `getFormattedAmount()` → VND format
- `app/code/Secomm/VietQr/Block/Info/VietQr.php` — admin order view amount → VND format
- `app/code/Secomm/VietQr/view/frontend/layout/sales_email_order_items.xml` — inject `vietqr_vnd_amount` layout argument
- `app/code/Secomm/VietQr/view/frontend/templates/email/payment-info.phtml` — email QR link amount + display → VND
- `app/code/Secomm/VietQr/Test/Unit/Model/VndAmountTest.php` (new) — 4 tests
- `app/code/Secomm/VietQr/CHANGELOG.md` — 1.0.4 entry
- `.ai/records/bugs/BUG-4BX0CK.md` (new) — canonical record

## Verification run (PHP 8.3 container, image `markoshust/magento-php:8.3-fpm-xdebug-7`, src mounted `/opt/project`)

### 1. Syntax lint — all PASS

```
php -l → No syntax errors detected:
  Model/VndAmount.php, Model/VietQr/RequestBuilder.php, Observer/GenerateQrAfterOrder.php,
  Block/PaymentInfo.php, Block/Info/VietQr.php, Test/Unit/Model/VndAmountTest.php,
  view/frontend/templates/email/payment-info.phtml
simplexml_load_file(sales_email_order_items.xml) → XML OK
```

### 2. Unit tests — 4/4 PASS

```
$ cd dev/tests/unit && php ../../../vendor/bin/phpunit --testsuite Magento_Unit_Tests_App_Code --filter VndAmountTest
OK, but there were issues!
Tests: 4, Assertions: 4, PHPUnit Warnings: 10, PHPUnit Deprecations: 25.
```

Warnings/deprecations are pre-existing core-test issues (GraphQl, Sales, Store, Ups test classes), unrelated to this change.

### 3. Project validator — record PASS

```
$ .ai/bin/project-ai-validate --check-records --check-specs
BUG-4BX0CK: no findings (FAIL count dropped 30 → 28; remaining 28 are pre-existing records of other work items)
```

### 4. Blast radius greps

- `getFormattedAmount()` consumers: `payment/view.phtml` ×2 only — fixed at block source.
- `vietqr_amount` consumers: Observer (write), PaymentInfo (write/read), Info block (read), email template (read) — all patched.
- `view/frontend/web/js/` — no amount/currency usage (clean).
- `PaymentInfo` instantiated via DI (controller constructor + layout block) — new typed constructor param auto-injects, no manual `new` call sites.

## Known limitations / deferred

- Orders placed before the fix keep the stale `vietqr_amount` + QR (regeneration = TL decision).
- QR retry consolidation (observer vs `PaymentInfo::loadPaymentInfo()` duplication) left as-is.
- Local DB stack for this project was not running during dev (running `layup-db-1` belongs to the LayUp project) — runtime QC must run on the correct environment/staging.

## Outstanding (human gates)

1. Tier 2 SA/TL code review — approve fail-closed approach.
2. Confirm currency-rate maintenance source for EN store view (not needed if website base = VND).
3. Decide: legacy orders QR regeneration; round vs ceil for converted fraction.
4. L3 QC: place orders on VI + EN store views, scan real QR, verify amounts; rate-missing fallback test.
